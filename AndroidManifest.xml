<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example"
          android:versionCode="1"
          android:versionName="1.0">
    <uses-sdk android:minSdkVersion="12"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application android:label="@string/app_name" android:icon="@drawable/oldvartalaap">
        <activity android:name="MyActivity"
                  android:label="@string/app_name">


        </activity>
        <activity android:name="login.DisplayLoginActivity"
                  android:label="Vartalaap"
                  android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>
        <activity android:name="roster.DisplayRosterActivity"
                  android:label="RosterActivity"
                  android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
                  android:launchMode="singleTask">
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value="Log.DisplayLoginActivity"/>

        </activity>
        <activity android:name="accounts.DisplayAccountsActivity"
                  android:label="AccountsActivity"
                  android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
                  android:launchMode="singleTask">
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value="Log.DisplayAccountsActivity"/>

        </activity>
        <activity
                android:name="message.MessageActivity"
                android:label="MessageActivity" >
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value="Log.DisplayRosterActivity" />
        </activity>
    </application>
</manifest>